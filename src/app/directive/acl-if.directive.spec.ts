import { AclIfDirective } from './acl-if.directive';

describe('AclIfDirective', () => {
  it('should create an instance', () => {
    const directive = new AclIfDirective();
    expect(directive).toBeTruthy();
  });
});
